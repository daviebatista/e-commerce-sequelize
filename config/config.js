module.exports = {
  dialect: "", // Inserir o banco de dados de sua preferência.
  host: "localhost", // -> Host padrão.
  username: "", // -> Inserir o seu usuário do banco de dados local.
  password: "", // -> Inserir a chave de segurança local.
  database: "ecommerce-davie", // -> Nome padrão do banco de dados.
  define: {
      timestamps: true,
  },
};